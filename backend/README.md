# Neuromind AI – Backend

Node.js + Express API for the Neuromind AI chat app.

## Tech Stack
- Node.js, Express, TypeScript
- PostgreSQL (or your DB) – *adjust if not used*
- JWT authentication
- Railway deployment

## Setup
```bash
git clone <repo-url>
cd backend   # or wherever package.json is
npm install